package one;

import java.util.Collections;
import java.util.HashMap;
import java.util.Map;
import java.util.stream.Stream;
import util.stream.Stream;

public class StreamSumExample {
    public static void main(String[] args) {
    	
        Map<String, Integer> map = new HashMap<>();
        map.put("Banana", 5);
        map.put("Orange", 7);

        // Sum all values
        
//      Map.of("koplo",90);
        System.out.println(map.entrySet());
      map = Map.of("Apple", 10,"wdwewe", 10,"qweded", 10,"rgth", 10);
      System.out.println(map.entrySet());
      
      
      
      
//        map.values().stream().filter(g -> g>5).collectf)
        
//        System.out.println("Total Sum: " + sum); // Output: 22
      
      Collections o = null; 
      
      
      Stream  n =null;
      
      collect()
      
      for(Map. j : map.entrySet()) {
    	  
      }
    }
}
